<?php
/**
 * Production application override.
 * Copy to config/app.local.php on the server and fill in real values.
 * config/app.local.php is gitignored — not pulled by deploys.
 */

return [
    'debug'          => false,
    'base_url'       => 'https://aviatortutor.com',
    'encryption_key' => 'REPLACE_WITH_32+_RANDOM_CHARS_BEFORE_DEPLOY',

    // Email verification (Phase 5)
    // Set to false during cutover so existing users without email_verified_at can still log in.
    'require_email_verification' => true,

    // Stripe (Phase 6) — leave blank to keep online checkout disabled.
    // Get keys from https://dashboard.stripe.com/apikeys
    'stripe_secret_key'      => '', // sk_live_… or sk_test_…
    'stripe_publishable_key' => '', // pk_live_… or pk_test_…
    'stripe_webhook_secret'  => '', // whsec_… from the webhook endpoint setup

    // Anthropic Claude (AI lesson generation — Phase 2+).
    // Get key from https://console.anthropic.com/settings/keys
    // Without this, /admin/ai-test will refuse to submit.
    'anthropic_api_key'      => '', // sk-ant-…
    // Optional: override model defaults if you want to bump generations.
    // 'anthropic_model_chunk'   => 'claude-sonnet-4-5',
    // 'anthropic_model_outline' => 'claude-opus-4-5',
];
